// The Javascript file to maintain the div and the view of the page and nav
// bars.

/*
 * $(document).ready(function(){ var $divView = $('div.chatinfo'); var
 * innerHeight = $divView.removeClass('chatinfo').height();
 * $divView.addClass('chatinfo');
 * 
 * $('div.slide').click(function() { $('div.view').animate({ height:
 * (($divView.height() == 100)? innerHeight : "100%") }, 500); return false; });
 * });
 * 
 */
/*
 * $(document).ready(function(){ (function postUsers(){ $.ajax({ method: "POST",
 * url: baseUrl + 'users', data: { fullName: "John", } }) .done(function( msg ) {
 * alert( "Data Saved: " + msg );
 * 
 * });
 * 
 * });
 *  })
 * 
 * 
 * 
 * //If user submits the form $(document).ready(function() {
 * $("#submitmsg").click(function() { var clientmsg = $("#usermsg").val();
 * 
 * $.post("/messages", { text : clientmsg }); $("#usermsg").attr("value", "");
 * return false; }); });
 * 
 */
